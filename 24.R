
boxplot(mpg ~ cyl, data=mtcars,
        main="MPG ~ CYL of mtcars",
        xlab="CYL of mtcars",
        ylab="MPG of mtcars",
        col=c("red", "blue", "yellow")
        )

mtcars$mpg

mtcars$cyl

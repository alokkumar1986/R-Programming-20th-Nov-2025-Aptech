mtcars

dim(mtcars)

summary(mtcars)


#min() : minimum of any column
#max() : maximum of any column
#mean() : Average of any column
#median() : middle of any column
#mode() : The maximum occurance of a value of any column

rownames(mtcars)

mtcars$mpg

sort(mtcars$mpg)

min(mtcars$mpg)
max(mtcars$mpg)
mean(mtcars$mpg)
mode(mtcars$mpg)
median(mtcars$mpg)

